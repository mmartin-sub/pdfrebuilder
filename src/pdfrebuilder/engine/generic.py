# NOTE: Engine version and metadata are now provided by the PDF engine abstraction (PDFEngineBase.engine_info property).

OUTPUT_SCHEMA_VERSION = "1.0"  # Updated version to reflect structural changes
