# Example .env file for pdfrebuilder
# Copy this file to .env and fill in the values.

# Optional: For extra tests that use Google's Gemini API
GEMINI_API_KEY="your_gemini_api_key"

# Optional: Environment ID for different configurations (e.g., Dev, Prod)
ENV_ID="Dev"
